<% @subtitles.each do |subtitle| %>
    <tr>
        <td><%= subtitle.word %></td>
        <td><%= subtitle.title %></td>
        <td><%= subtitle.counter %></td>
        <td><%= 313 / subtitle.counter %> mins</td>
        <td><%= subtitle.syllable %></td>
    </tr>
<% end %>

<%= will_paginate @subtitles %>
